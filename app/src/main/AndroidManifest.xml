<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.p8_app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.P8App">
        <activity
            android:name=".FarmerAdditionActivity"
            android:label="@string/title_activity_framer_addition"
            android:theme="@style/Theme.P8App.NoActionBar"></activity>
        <activity android:name=".UserLogin"></activity>
        <activity
            android:name=".FrontpageActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.P8App.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FoodSelectionModel" />
        <activity android:name=".MainActivity" />
        <activity android:name=".CreateActivity" />
        <activity android:name=".StartActivity" />
    </application>

</manifest>